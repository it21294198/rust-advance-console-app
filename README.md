#### Compile and run
```bash
cargo run -- [args]
```

#### Just build ,doesn't run
```bash
cargo build
```

#### Build with release
```bash
cargo build --release
```

#### Build just .exe(for window)
```bash
./main -- [args] #(for linux or mac)
.\main.exe -- [args] #(for windows)
```